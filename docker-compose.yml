version: '3.4'

services:
  ultra.web:
    image: ultraweb
    build:
      context: .
      dockerfile: Ultra.Web/Dockerfile
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
